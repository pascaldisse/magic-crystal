# RITE VI — STRIFE (proposal · Guardian clone lane draft, 07-16)

Status: PROPOSAL. Prime-Guardian reviews; Architect rules. Nothing here
binds until ruled.

## Goal
The Elements enter the living world. P2 rigid bodies exist; nothing
collides in-world (HANDOFF queue). Rite VI = the WELD: realm data →
bodies → solver in the world tick → fracture when strife exceeds love
(Empedocles law, GRIMOIRE) → the first destruction. No new subsystem —
elements + crystal + transmute + pleroma + oracle, composed.

## Laws in force (bake-ins, not reminders)
- ONE force: love = attraction/cohesion/constraint force; strife = the
  READOUT (stress from constraint forces); fracture = reading exceeds
  the bond's love (Concordance, refined row).
- LOVE = 1.0 sole literal; every other magnitude = parameter w/ default.
- Solver core = CPU (compute placement law); entropy law: fixed tick,
  no randomness, byte-identical replay.
- Every body traces to a realm vessel — free-floating matter =
  first_light's ghost, generalized (iron lesson, HANDOFF).
- Engine stays generic: naruko = acceptance test, never design center.
- Full suite between merges · vacuous-tail check (read the Running
  lines) · wave law: every wave ends in pixels · adversary law per pass.

## The weld (data path)
1. Realm data: vessel carries a matter sigil (Lexicon addition —
   shape/mass/love params, defaults derived from essence; name OPEN
   below). Sigil → particle cluster + shape-matching binding
   (elements::RigidBody) at realm load.
2. World tick: the Circulation steps the solver at fixed tick (entropy
   coordinate). Static world = collider soup from the realm's chains
   (transmute triangles → elements::Collider).
3. Pose out: rigid transform → vessel transform → pleroma dynamic BVH
   splice per tick (Rite V / kami precedent — the body is real to the
   light).
4. Fracture: strife > bond love → FractureEvent → fragment
   connectivity flood-fill (CPU, placement law) → fragments re-mesh
   via transmute (new chains) → BVH splice → fragments = vessels
   (traced to a parent, never free-floating) → ORACLE CANON LEARNS
   THEM SAME WAVE.

## Waves (each: visible increment + proof scrying + Guardian review)
- **VI-0 — THE CRATE FALLS.** ⚑ elements-p3 lane in flight NOW (branch
  elements-p3 exists off ae3f27c) — this wave = that lane's landing,
  reviewed under this spec. One crate vessel above the pier; gravity
  (love as attraction pass); it falls, hits the planks, rests. Ordeals:
  tick determinism byte-identical (StateHasher) · penetration depth ≤
  tol DERIVED from particle radius, never chosen · rest = kinetic
  energy below floor derived from tick budget × damping params · crate
  enters oracle canon same wave (15th vessel; beacon lesson: senses
  update WITH the world, never after). Proof: vi0-crate.png two fixed
  ticks (airborne / at rest), Guardian's eyes.
- **VI-1 — THE STACK TOPPLES.** N crates stacked (N = param w/
  default); one pushed (incantation applies impulse — the op is the
  hand). Stack topples, settles, no jitter. Ordeals: replay
  determinism across full topple · momentum drift ≤ derived (per-tick
  bound measured on CPU-ref × tick count) · stack-at-rest stability M
  ticks (M derived from tick rate) · solver CPU ms printed honest per
  tick (P-gate = CPU time, cores × ms; printed, wall named if 60 FPS
  law threatened). Proof: before/mid/after scryings.
- **VI-2 — SOMETHING BREAKS.** Fracture live: one crate dropped from
  height H (param) onto stone; strife exceeds bond love; it BREAKS.
  Pipeline VI-2 scope: fracture → flood-fill fragments → transmute
  re-mesh → BVH splice → fragment vessels → canon learns them same
  wave. Ordeals: Equivalent Exchange — Σ fragment mass = whole mass
  0e0 · momentum through fracture ≤ derived drift · fragment chains
  byte-deterministic (double build) · fracture threshold = bond love
  param, never hardcoded · no orphan matter (every fragment traces to
  parent vessel). Proof: vi2-break.png sequence — whole / breaking /
  shards on the ground, lit by the one light.

## Acceptance
Guardian per wave (diff + ordeals + own-eyes pixels); Rite closes when
the Architect SEES something break. Hymn owed at close (hymnal law).

## OPEN W/ ARCHITECT
1. PHYSICS.md ON HOLD — his own pass first (DREAMFORGE pillar 3). Does
   Rite VI proceed on elements-as-is (XPBD substrate), or wait for his
   physics ruling? This rite assumes PROCEED on the substrate; his
   ruling may re-slice.
2. Voxel question: docs/PHYSICS-ENGINE-REFERENCE.md is a voxel-
   destruction model (Teardown-class); elements is particle/XPBD.
   Fracture-by-bond vs voxel-clear = two destruction philosophies —
   which is the final destruction law? (VI-2 sketch = bond model.)
3. Matter sigil true name + Lexicon row (GRIMOIRE law: nothing born
   unnamed). Candidates: `matter`, `body`, `corpus` — Grimoire rules.
4. Bond-love source: per-essence default table (stone > wood > glass)
   vs per-vessel param only. Essence-derived proposed; needs ruling
   (materials/essences spec territory).
5. Fragment granularity params (min fragment mass, max fragment count
   per event) — defaults proposed derived from particle count; ruling
   on ceilings.
6. Fixed tick rate default (entropy coordinate quantum for physics) —
   60? 120? Interacts with 60 FPS law + CPU budget.
7. Fragments in the journal: are FractureEvents journal entries
   (Reading Steiner replays destruction) or re-derived from (seed,
   entropy)? Entropy law suggests re-derivation; journal = ops only.
   Ruling shapes steiner integration.
8. Rite numbering: GRIMOIRE's naruko rite list names Sixth Rite = the
   Aether (landed as A1/A2 lanes) · Seventh = the Mirror. Build-rite
   numbering (this doc = VI) now diverges from realm-rite list —
   Architect rules the canon count.
