# MIND — THE ONE MIND + MONAD (ruling packet, 07-18; research: DF Bible ·
# exemplars · TTRPG/GAIA/MyRobot substrate — docs/research/ + room ledger)

STATUS: PROPOSAL. The Architect rules; no code before his word.

## Laws inherited (sealed, not re-argued)
Offline whole; LLM never required (voice = optional ANE package) · one mind
system, organs not rivals · deterministic: state=f(seed,entropy,journal) ·
60fps M1 untouchable (mind never on the frame's critical path) · package
law: mind = package in the mind socket; MONAD = constitutional, in the
crystal core · study-never-import · IRON · death rule for every net.

## THE LOOP (one tick of one agent; all evidence converged on this shape)
```
world snapshot + event tail
→ update CONDITIONING VECTORS      (who I am right now, as numbers)
→ update SALIENCE LEDGER           (what I know/notice, bounded)
→ enumerate AFFORDANCES            (world advertises acts, typed data)
→ HARD MASK                        (legality/body/social/emotion — before any net)
→ SMALL NET ranks survivors        (the only learned judgment; journaled)
→ PACING selects act|wait          (MONAD budget as context, never command)
→ ACT DOOR                         (bounded command; engine owns the body)
→ deterministic resolution → canonical ops → journal
→ memory/ledger update             (consolidation, not reflection)
```

## Organs (evidence per bone)
- CONDITIONING VECTORS (DF-calibrated, dims = params): personality facets +
  values + goals (DF: 50·0-100 + 33·±50 + dreams — adopt structure, dims
  ruled by him) · needs w/ decay + focus ratio (DF: 30, satisfy→400,
  met:unmet → ±skill) · affect (valence/arousal/stress) · body convictions
  (rain: !BACK/!FLOAT/!SINK…) · clock/goal/scene. Numbers in, never prose.
- AFFORDANCE WORLD (Sims law: the WORLD advertises): every possible act =
  data {verb, actor, target, precond, effects, cost, tags} — TTRPG's 14
  semantic ops = the proven op vocabulary; no hidden bespoke AI paths.
- HARD MASK BEFORE NET (FEAR legality + MyRobot emotion-gates-affordances):
  death/reach/body/status/resources/consent/social permission → candidate
  set; the net NEVER sees an illegal act. Emotion mutates the mask (proven
  mechanism, Emotions.cs) — feel changes what is THINKABLE, not just scores.
- THE SMALL NET: ranks legal candidates only; per-agent conditioning vector
  input; journal = {candidates, scores, chosen index} → bit-replayable.
  Teacher = M1's hand-coded utility (Sims curves + salience); trained
  in-loop per doctrine; death rule judges vs the teacher it replaces.
- SALIENCE LEDGER + MEMORY (DF 8+8+core · Nemesis named-rivals · MyRobot
  64-ring, fixed): bounded slots {entity, lastSeen, lastPose, intrinsic,
  interest, eventWeight, confidence, expires}; promotion by salience;
  CORE memories = sparse permanent conditioning deltas (DF's one true
  trick: memory that changes who you are). Consolidation = typed rules,
  never prose, never unbounded.
- SOCIAL CONTRACTS (Versu practices + TTRPG relationships): explicit state
  {parties, obligation, consent, status, evidence op, expiry}; grudges
  persist (DF law); disposition = scalar + history; witnessed commitments
  = journal facts.
- REFLEX ARCS = ENGINE-OWNED (MyRobot locomotion law): balance, grounding,
  collision correction, emergency halt run regardless of cognition; the
  mind reads resulting events, never micromanages the body.
- SENSES/ACT DOORS (native, typed — GAIA seam reborn in the crystal):
  SenseEvent{Proprio, Percept, Appeared/Moved/Left, Heard(spatial-stamped),
  Felt, WorldOp} · ActCommand{MoveTo, Walk, Face, Grab, Drop, Speak, Use} →
  ActResult{status, pose, ops, convictions}. Speech stamped at source pose.
- PATHFINDING: classical (A*/flow fields) = authority; nets = cost/steering
  garnish only (field verdict: no shipped neural nav beats it).
- MONAD (crystal core, GRIMOIRE §MONAD): world director — tension/
  opportunity BUDGET (RimWorld/L4D lineage) + event authoring through the
  SAME ops door; reads everything, commands no agent's mind, fabricates no
  memory. Tarn's fairness law verbatim: disasters follow hubris/folly or
  offer prevention. Judgment = core; tongue = ANE voice package.

## Determinism
Every net choice journaled (candidates+scores+index) · dice = kami hash ·
voice-package outputs enter as journaled events (replay = same world even
if the tongue changes) · fixed iteration order · cohort scheduling seeded.

## Performance (DF choir, adopted)
Near/visible agents 10-20Hz · distant 0.2-2Hz aggregated · staggered
cohorts (DF: jobs/100 ticks, seasons/10, cleanup/1000) · dirty flags skip
sleeping regions · mind runs OFF the render thread entirely; frame never
waits for a thought.

## Staging (each gate = ordeals + adversary + his word)
M0 crystal extraction (mind socket exists) — prerequisite, already owed.
M1 affordance world + hard mask + HAND-CODED utility (the teacher; no net).
   Gate: agents live in playground w/ needs; every act legal; replay exact.
M2 conditioning vectors + salience ledger + core-memory deltas.
M3 the small net ranks (trained in-loop from M1 teacher; death rule).
M4 social contracts + MONAD director (budget + events + fairness law).
M5 voice package (ANE, offline, async, journaled) — optional forever.

## His rulings needed
1. Conditioning dims: adopt DF's 50+33 verbatim, or reduced set (my rec:
   start reduced ~16+8, grow by measurement — DF's own effects are half
   unknown even to its wiki).
2. Mind tick cadence defaults (rec: 10Hz near / 1Hz far, params).
3. MONAD's event vocabulary v1 (rec: TTRPG's proven 14 ops + spawn/weather
   /clock through the existing door).
4. Name for the mind system (spine name = his to give; "THE ONE MIND" =
   placeholder).
