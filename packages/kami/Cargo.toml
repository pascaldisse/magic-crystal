[package]
name = "kami"
version = "0.1.0"
edition = "2021"
description = "DreamForge KAMI: data-driven behavior ticks over the crystal ECS"
license = "MIT"

[dependencies]
crystal = { path = "../../crates/crystal" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
