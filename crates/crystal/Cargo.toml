[package]
name = "crystal"
version = "0.1.0"
edition = "2021"
description = "the Magic Crystal — DreamForge core: ECS, protocol, scheduler, and package registry"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
